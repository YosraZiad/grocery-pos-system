# مقترح خطة العمل: هيكلة واجهات ERP وتطبيق نظام الوصول المبني على الصلاحيات (PBAC)

**مقدمة:**
الهدف من هذه الخطة هو تنظيم واجهات تطبيق نقطة البيع (POS) الحالي لتتبع أفضل الممارسات في تصميم أنظمة الـ ERP ذات البيانات الكثيفة (Data-Dense Layouts)، مع تأمين النظام باستخدام نظام التفويض المبني على الصلاحيات (PBAC) لضمان مرونة تشغيلية كاملة للمنشأة دون الحاجة لتغيير الكود مستقبلاً عند تعديل أدوار الموظفين.

---

## 🛠️ أولاً: إنشاء وتحسين المخططات الهيكلية للواجهات (Establish Layouts)

سنقوم بتنظيم شاشات النظام الحالية لتتبع 6 مخططات هيكلية رئيسية:

1. **مخطط لوحة التحكم (Dashboard Layout)**:
   - _الشاشة المستهدفة_: الصفحة الرئيسية (Home).
   - _الوصف_: يعتمد على شبكة مرنة (Flexible Grid) تعرض مؤشرات الأداء الأساسية (KPIs)، لمحات بيانية سريعة للمبيعات، وتنبيهات النواقص بالمخزون.
2. **مخطط القائمة والتفاصيل (Master-Detail / Split-Screen)**:
   - _الشاشات المستهدفة_: صفحة إدارة الموردين (Suppliers) والورديات (Shifts).
   - _الوصف_: تقسيم الشاشة رأسياً؛ الجانب الأيمن يعرض قائمة سريعة بالموردين، والجانب الأيسر يعرض كامل التفاصيل والعمليات للمورد المحدد لتسريع العمل وتجنب كثرة التنقل بين الصفحات.
3. **مخطط النماذج والمدخلات (Form & Input Layouts)**:
   - _الشاشات المستهدفة_: الملف الشخصي (Profile) وإعدادات النظام (Settings).
   - _الوصف_: استخدام النموذج المبوب (Tabbed Layout) لتقسيم حقول الإدخال الكثيرة إلى تبويبات واضحة ومنظمة تمنع تشتت المستخدم أثناء الإدخال.
4. **مخطط الجداول والبيانات الضخمة (Data Table / Grid Layouts)**:
   - _الشاشات المستهدفة_: قائمة المنتجات (Products) وقائمة الفواتير السابقة (Sales List).
   - _الوصف_: جداول تفاعلية متناسقة تدعم التصفية المتقدمة (Filters)، البحث الفوري، وأزرار التصدير (Excel/PDF).
5. **مخطط بطاقة العنصر (Profile / Entity View Layout)**:
   - _الشاشات المستهدفة_: تفاصيل الفاتورة الفردية (Sale Details) وتفاصيل المنتج (Product Card).
   - _الوصف_: صفحة مرجعية كاملة تعرض معلومات عنصر واحد بشكل تفصيلي مع إبراز حالته وأزرار الأكشن الخاصة به في الهيدر.
6. **الهيكل البنائي العام للتطبيق (Shell Layout)**:
   - _الوصف_: هيكل عام ثابت يربط كل ما سبق؛ يتكون من شريط جانبي (Sidebar) للتنقل وقابل للطي لتوفير مساحة للبيانات، وشريط علوي (Navbar) للمعلومات السريعة، ومنطقة محتوى متغيرة.

---

## 🔒 ثانياً: بناء نظام الأدوار والصلاحيات في الخلفية (Permissions & Roles System)

\*\*\* Spatie Permission (Package Laravel)
الانتقال من الفحص التقليدي المبني على الدور (RBAC) إلى الفحص الدقيق المبني على الصلاحية (PBAC):

1. **تعريف الصلاحيات الفردية (Fine-Graded Permissions)**:
   - بدلاً من تصنيف الصلاحيات للمدير فقط، سنقوم بتعريف صلاحية لكل إجراء برمجياً، مثل:
     - `view products`, `create products`, `edit products`, `delete products`
     - `view settings`, `edit settings`
     - `view reports`, `export reports`
2. **التحميل المسطح للصلاحيات (Flat Permissions Array)**:
   - عند تسجيل دخول الكاشير أو طلب بياناته، يقوم السيرفر بجمع كافة صلاحيات الدور المسند إليه وتمريرها في كائن الاستجابة كقائمة نصية مسطحة (مثال: `all_permissions: ["view products", "create sales"]`).
   - هذا يسهل على الواجهة الأمامية التحقق من الصلاحيات بسرعة وكفاءة.

---

## 🚦 ثالثاً: دمج شروط الصلاحيات في المخططات للتحكم بالمحتوى (PBAC Integration)

ربط الواجهات وتأمينها بناءً على قائمة صلاحيات المستخدم المسجل دخوله:

1. **حارس مسارات التصفح (Route Guarding)**:
   - حماية الروابط في المتصفح؛ فمثلاً إذا حاول كاشير لا يملك صلاحية `view settings` الدخول مباشرة لكتابة الرابط `/settings` في المتصفح، يتم منعه تلقائياً وتوجيهه لصفحة "غير مصرح لك بالوصول".
2. **حجب العناصر داخل الصفحات (Component-Level Checking)**:
   - استخدام مكون برمجي فاحص (مثل `<ProtectedComponent>`) لإخفاء أو إظهار أزرار معينة داخل الجداول أو النماذج.
     - _مثال_: يرى الكاشير صفحة المنتجات (لأنه يملك `view products`) ولكن يختفي منه تماماً زر "إضافة منتج" و"حذف" لعدم امتلاكه الصلاحيات المقابلة لها.
3. **ديناميكية شريط التنقل (Dynamic Navigation Bar)**:
   - إخفاء أو إظهار التبويبات والموديولات في شريط التنقل الجانبي (Sidebar) تلقائياً؛ فلا يظهر تبويب "المستخدمين" أو "التقارير" في قائمة الكاشير الجانبية إلا إذا كان يملك صلاحية رؤيتها.

General Layout

- top nav
- Sidebar
- Content

  Dashboard

##### General Layout

Resources/Index

##### General+Title&actions+Statestics+Filteration+contents(List-grid)

Forms

##### General+Title&actions+Form

Resource

##### General+Title&actions+Content

Reports
Print

##### Header+content+footer

Tasty
Delecious (لذيذ)
