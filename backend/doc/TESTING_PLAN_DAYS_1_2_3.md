# 🧪 خطة اختبار شاملة - الأيام 1، 2، 3

## 📋 نظرة عامة

هذه خطة اختبار شاملة لجميع الميزات المنجزة في الأيام الثلاثة الأولى من المشروع.

---

## 🔧 إعداد البيئة

### المتطلبات
- PHP >= 8.2
- Composer
- MySQL >= 8.0
- Node.js >= 18
- Git

### إعداد Backend

```bash
cd c:\wamp64\www\grocery-pos-backend

# تثبيت الحزم
composer install

# إعداد .env
cp .env.example .env
# تحديث إعدادات قاعدة البيانات في .env

# تشغيل Migrations و Seeders
php artisan migrate:fresh --seed

# تشغيل الخادم
php artisan serve
```

Backend سيعمل على: `http://localhost:8000`

### إعداد Frontend

```bash
cd c:\wamp64\www\grocery-pos-frontend

# تثبيت الحزم
npm install

# إعداد .env
# إنشاء ملف .env وأضف:
# VITE_API_URL=http://localhost:8000/api

# تشغيل التطبيق
npm run dev
```

Frontend سيعمل على: `http://localhost:5173`

---

## 🧪 اختبارات اليوم الأول

### 1. اختبار Backend API

#### 1.1 Test Endpoint
```bash
curl http://localhost:8000/api/test
```

**النتيجة المتوقعة:**
```json
{
  "message": "API is working",
  "tenant_id": null
}
```

**✅ النجاح**: يتم إرجاع JSON response

#### 1.2 اختبار CORS
1. افتح المتصفح وانتقل إلى `http://localhost:5173`
2. افتح Developer Tools (F12)
3. اذهب إلى Console
4. لا يجب أن تظهر أخطاء CORS

**✅ النجاح**: لا توجد أخطاء CORS في Console

#### 1.3 اختبار Multi-Tenant Middleware

**بدون tenant_id:**
```bash
curl http://localhost:8000/api/test
```

**النتيجة المتوقعة:**
```json
{
  "error": "Tenant not identified"
}
```

**✅ النجاح**: يتم إرجاع خطأ 403

**مع tenant_id في header:**
```bash
curl -H "X-Tenant-ID: 1" http://localhost:8000/api/test
```

**النتيجة المتوقعة:**
```json
{
  "message": "API is working",
  "tenant_id": "1"
}
```

**✅ النجاح**: يتم إرجاع tenant_id بشكل صحيح

---

## 🧪 اختبارات اليوم الثاني

### 2. اختبار Authentication (Backend)

#### 2.1 تسجيل الدخول كمدير

```bash
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "password": "password"
  }'
```

**النتيجة المتوقعة:**
```json
{
  "message": "Login successful",
  "user": {
    "id": 1,
    "name": "مدير النظام",
    "email": "admin@example.com",
    "tenant_id": 1,
    "roles": [{"id": 1, "name": "admin"}]
  },
  "token": "1|...",
  "tenant_id": 1
}
```

**✅ النجاح**: 
- يتم إرجاع token
- يتم إرجاع بيانات المستخدم
- يتم إرجاع tenant_id
- يتم إرجاع roles

#### 2.2 تسجيل الدخول ككاشير

```bash
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "cashier@example.com",
    "password": "password"
  }'
```

**النتيجة المتوقعة:**
```json
{
  "message": "Login successful",
  "user": {
    "id": 2,
    "name": "كاشير",
    "email": "cashier@example.com",
    "tenant_id": 1,
    "roles": [{"id": 2, "name": "cashier"}]
  },
  "token": "2|...",
  "tenant_id": 1
}
```

**✅ النجاح**: يتم تسجيل الدخول بنجاح

#### 2.3 بيانات المستخدم (me)

```bash
# احفظ الـ token من الخطوة السابقة
TOKEN="1|..."

curl -X GET http://localhost:8000/api/auth/me \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-Tenant-ID: 1"
```

**النتيجة المتوقعة:**
```json
{
  "user": {
    "id": 1,
    "name": "مدير النظام",
    "email": "admin@example.com",
    "tenant_id": 1,
    "roles": [...],
    "permissions": [...]
  }
}
```

**✅ النجاح**: يتم إرجاع بيانات المستخدم مع roles و permissions

#### 2.4 تسجيل الخروج

```bash
curl -X POST http://localhost:8000/api/auth/logout \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-Tenant-ID: 1"
```

**النتيجة المتوقعة:**
```json
{
  "message": "Logged out successfully"
}
```

**✅ النجاح**: يتم تسجيل الخروج بنجاح

#### 2.5 اختبار Protected Route بدون Token

```bash
curl -X GET http://localhost:8000/api/auth/me
```

**النتيجة المتوقعة:**
```json
{
  "message": "Unauthenticated."
}
```

**✅ النجاح**: يتم إرجاع خطأ 401

### 3. اختبار Authentication (Frontend)

#### 3.1 تسجيل الدخول

1. افتح `http://localhost:5173`
2. سيتم توجيهك تلقائيًا إلى `/login`
3. استخدم:
   - Email: `admin@example.com`
   - Password: `password`
4. اضغط "تسجيل الدخول"

**✅ النجاح**: 
- يتم تسجيل الدخول بنجاح
- يتم توجيهك للصفحة الرئيسية
- يتم حفظ token في localStorage
- يتم حفظ tenant_id في localStorage

#### 3.2 التحقق من Layout

بعد تسجيل الدخول، تحقق من:
- ✅ اسم المستخدم يظهر في Header
- ✅ الدور (admin أو cashier) يظهر في Header
- ✅ زر "تسجيل الخروج" موجود

#### 3.3 تسجيل الخروج

1. اضغط على "تسجيل الخروج"
2. تحقق من:
   - ✅ يتم توجيهك إلى `/login`
   - ✅ يتم مسح token من localStorage
   - ✅ يتم مسح tenant_id من localStorage

#### 3.4 Protected Routes

1. حاول الوصول مباشرة إلى `http://localhost:5173` بدون تسجيل دخول
2. تحقق من:
   - ✅ يتم توجيهك تلقائيًا إلى `/login`

#### 3.5 التسجيل (Register)

1. اذهب إلى `/register`
2. املأ النموذج:
   - الاسم: "مستخدم تجريبي"
   - البريد: "test@example.com"
   - كلمة المرور: "password123"
   - تأكيد كلمة المرور: "password123"
3. اضغط "إنشاء حساب"

**✅ النجاح**: 
- يتم التسجيل بنجاح
- يتم توجيهك للصفحة الرئيسية
- يتم حفظ token في localStorage

---

## 🧪 اختبارات اليوم الثالث

### 4. اختبار Categories (Backend)

#### 4.1 عرض جميع الأقسام

```bash
curl -X GET http://localhost:8000/api/categories \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-Tenant-ID: 1"
```

**النتيجة المتوقعة:**
```json
{
  "data": [
    {
      "id": 1,
      "name": "...",
      "description": "...",
      "products_count": 0
    }
  ]
}
```

**✅ النجاح**: يتم إرجاع جميع الأقسام

#### 4.2 إضافة قسم جديد

```bash
curl -X POST http://localhost:8000/api/categories \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-Tenant-ID: 1" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "مشروبات",
    "description": "مشروبات غازية وعصائر"
  }'
```

**النتيجة المتوقعة:**
```json
{
  "message": "Category created successfully",
  "data": {
    "id": 1,
    "name": "مشروبات",
    "description": "مشروبات غازية وعصائر",
    "tenant_id": 1
  }
}
```

**✅ النجاح**: يتم إنشاء القسم بنجاح

#### 4.3 تعديل قسم

```bash
curl -X PUT http://localhost:8000/api/categories/1 \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-Tenant-ID: 1" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "مشروبات محدثة",
    "description": "وصف محدث"
  }'
```

**✅ النجاح**: يتم تحديث القسم بنجاح

#### 4.4 حذف قسم

```bash
curl -X DELETE http://localhost:8000/api/categories/1 \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-Tenant-ID: 1"
```

**✅ النجاح**: يتم حذف القسم بنجاح

#### 4.5 حذف قسم يحتوي على منتجات

1. أنشئ منتج في قسم معين
2. حاول حذف القسم

**النتيجة المتوقعة:**
```json
{
  "message": "Cannot delete category with existing products"
}
```

**✅ النجاح**: يتم منع حذف القسم

### 5. اختبار Products (Backend)

#### 5.1 عرض جميع المنتجات

```bash
curl -X GET "http://localhost:8000/api/products?page=1&per_page=20" \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-Tenant-ID: 1"
```

**النتيجة المتوقعة:**
```json
{
  "data": [...],
  "links": {...},
  "meta": {...}
}
```

**✅ النجاح**: يتم إرجاع المنتجات مع pagination

#### 5.2 إضافة منتج جديد

```bash
curl -X POST http://localhost:8000/api/products \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-Tenant-ID: 1" \
  -H "Content-Type: application/json" \
  -d '{
    "category_id": 1,
    "name": "كوكا كولا",
    "barcode": "123456789",
    "purchase_price": 2.5,
    "sale_price": 3.5,
    "quantity": 100,
    "expiry_date": "2026-12-31",
    "min_stock_alert": 10,
    "min_expiry_alert": 7
  }'
```

**النتيجة المتوقعة:**
```json
{
  "message": "Product created successfully",
  "data": {
    "id": 1,
    "name": "كوكا كولا",
    "barcode": "123456789",
    ...
  }
}
```

**✅ النجاح**: يتم إنشاء المنتج بنجاح

#### 5.3 البحث عن منتج

```bash
curl -X GET "http://localhost:8000/api/products/search?q=كوكا" \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-Tenant-ID: 1"
```

**✅ النجاح**: يتم إرجاع نتائج البحث

#### 5.4 البحث بالباركود

```bash
curl -X GET "http://localhost:8000/api/products/search?q=123456789" \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-Tenant-ID: 1"
```

**✅ النجاح**: يتم إرجاع المنتج بالباركود

#### 5.5 تعديل منتج

```bash
curl -X PUT http://localhost:8000/api/products/1 \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-Tenant-ID: 1" \
  -H "Content-Type: application/json" \
  -d '{
    "category_id": 1,
    "name": "كوكا كولا محدثة",
    "quantity": 150
  }'
```

**✅ النجاح**: يتم تحديث المنتج بنجاح

#### 5.6 حذف منتج

```bash
curl -X DELETE http://localhost:8000/api/products/1 \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-Tenant-ID: 1"
```

**✅ النجاح**: يتم حذف المنتج بنجاح

### 6. اختبار Categories (Frontend)

#### 6.1 عرض الأقسام

1. سجل دخول كمدير
2. اذهب إلى `/categories`
3. تحقق من:
   - ✅ يتم عرض جميع الأقسام في جدول
   - ✅ يتم عرض عدد المنتجات في كل قسم

#### 6.2 إضافة قسم جديد

1. اضغط على "إضافة قسم جديد"
2. املأ النموذج:
   - الاسم: "مشروبات"
   - الوصف: "مشروبات غازية وعصائر"
3. اضغط "إضافة"

**✅ النجاح**: 
- يتم إضافة القسم بنجاح
- يظهر القسم الجديد في الجدول
- يتم إغلاق Modal

#### 6.3 تعديل قسم

1. اضغط على "تعديل" بجانب قسم
2. عدل البيانات
3. اضغط "تحديث"

**✅ النجاح**: يتم تحديث القسم بنجاح

#### 6.4 حذف قسم

1. اضغط على "حذف" بجانب قسم
2. أكد الحذف

**✅ النجاح**: يتم حذف القسم بنجاح

### 7. اختبار Products (Frontend)

#### 7.1 عرض المنتجات

1. اذهب إلى `/products`
2. تحقق من:
   - ✅ يتم عرض جميع المنتجات في جدول
   - ✅ يتم عرض معلومات المنتج (الاسم، القسم، الباركود، الأسعار، الكمية)
   - ✅ يتم عرض حالة المخزون (منخفض/متوفر)

#### 7.2 البحث عن منتج

1. اكتب في شريط البحث: "كوكا"
2. تحقق من:
   - ✅ يتم فلترة المنتجات حسب البحث

#### 7.3 الفلترة حسب القسم

1. اختر قسم من dropdown
2. تحقق من:
   - ✅ يتم عرض المنتجات في القسم المحدد فقط

#### 7.4 إضافة منتج جديد

1. اضغط على "إضافة منتج جديد"
2. املأ النموذج:
   - القسم: اختر قسم
   - اسم المنتج: "كوكا كولا"
   - الباركود: "123456789"
   - سعر الشراء: 2.5
   - سعر البيع: 3.5
   - الكمية: 100
   - تاريخ انتهاء الصلاحية: (اختياري)
3. اضغط "إضافة"

**✅ النجاح**: 
- يتم إضافة المنتج بنجاح
- يظهر المنتج الجديد في الجدول
- يتم إغلاق Modal

#### 7.5 تعديل منتج

1. اضغط على "تعديل" بجانب منتج
2. عدل البيانات
3. اضغط "تحديث"

**✅ النجاح**: يتم تحديث المنتج بنجاح

#### 7.6 حذف منتج

1. اضغط على "حذف" بجانب منتج
2. أكد الحذف

**✅ النجاح**: يتم حذف المنتج بنجاح

### 8. اختبار Global Scope

#### 8.1 اختبار Multi-Tenant Isolation

1. سجل دخول كمستخدم من tenant 1
2. أنشئ منتج
3. سجل دخول كمستخدم من tenant 2
4. حاول عرض المنتجات

**✅ النجاح**: 
- لا يمكن رؤية منتجات tenant 1 من tenant 2
- كل tenant يرى منتجاته فقط

---

## ✅ قائمة التحقق الشاملة

### Backend
- [ ] Laravel API يعمل على `http://localhost:8000`
- [ ] Migrations تم تشغيلها بنجاح
- [ ] Seeders تم تشغيلها بنجاح
- [ ] CORS يعمل بشكل صحيح
- [ ] Multi-Tenant Middleware يعمل
- [ ] تسجيل الدخول يعمل
- [ ] `/api/auth/me` يعمل مع token
- [ ] تسجيل الخروج يعمل
- [ ] Categories CRUD يعمل
- [ ] Products CRUD يعمل
- [ ] Products Search يعمل
- [ ] Global Scope يعمل (Multi-Tenant isolation)

### Frontend
- [ ] React App يعمل على `http://localhost:5173`
- [ ] صفحة Login تعمل
- [ ] صفحة Register تعمل
- [ ] تسجيل الدخول من Frontend يعمل
- [ ] Protected Routes تعمل
- [ ] Layout يعرض معلومات المستخدم
- [ ] تسجيل الخروج يعمل
- [ ] صفحة Categories تعمل
- [ ] صفحة Products تعمل
- [ ] إضافة/تعديل/حذف Categories يعمل
- [ ] إضافة/تعديل/حذف Products يعمل
- [ ] البحث في Products يعمل
- [ ] الفلترة حسب القسم تعمل

### Integration
- [ ] الاتصال بين Frontend و Backend يعمل
- [ ] Token يتم إرساله تلقائيًا في Requests
- [ ] tenant_id يتم إرساله تلقائيًا في Requests
- [ ] عند 401 Unauthorized، يتم توجيه المستخدم للـ login
- [ ] Error Handling يعمل بشكل صحيح

---

## 🐛 حل المشاكل الشائعة

### مشكلة CORS
- تأكد من أن `config/cors.php` يحتوي على `localhost:5173`
- تأكد من أن `config/sanctum.php` يحتوي على `localhost:5173` في `stateful`

### مشكلة Token
- تأكد من أن token يتم حفظه في localStorage
- تأكد من أن Axios interceptors يضيف token في header

### مشكلة tenant_id
- تأكد من أن tenant_id يتم حفظه في localStorage بعد تسجيل الدخول
- تأكد من أن TenantMiddleware يقرأ tenant_id من header أو user

### مشكلة Database
- تأكد من إعدادات `.env` لقاعدة البيانات
- تأكد من تشغيل `php artisan migrate:fresh --seed`

---

## 📊 تقرير الاختبار

بعد إكمال جميع الاختبارات، قم بملء هذا التقرير:

### النتائج
- **عدد الاختبارات**: ___
- **الاختبارات الناجحة**: ___
- **الاختبارات الفاشلة**: ___
- **نسبة النجاح**: ___%

### المشاكل المكتشفة
1. ___
2. ___
3. ___

### الملاحظات
___

---

**تاريخ الاختبار**: ___
**المختبر**: ___
