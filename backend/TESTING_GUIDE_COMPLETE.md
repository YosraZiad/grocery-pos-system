# 🧪 دليل الاختبار الشامل - من الواجهة والبوستمان

## 📋 نظرة عامة

هذا الدليل الشامل لاختبار جميع الميزات المنجزة (الأيام 1-5) من خلال:
- ✅ **Postman** - اختبار API مباشرة
- ✅ **Frontend** - اختبار من الواجهة

---

## 🔧 الإعداد الأولي

### 1. إعداد Backend

```bash
cd c:\wamp64\www\grocery-pos-backend

# تأكد من تثبيت الحزم
composer install

# تأكد من إعداد .env
# DB_CONNECTION=mysql
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=grocery_pos
# DB_USERNAME=root
# DB_PASSWORD=

# تشغيل Migrations و Seeders
php artisan migrate:fresh --seed

# تشغيل الخادم
php artisan serve
```

**✅ Backend يعمل على:** `http://localhost:8000`

### 2. إعداد Frontend

```bash
cd c:\wamp64\www\grocery-pos-frontend

# تأكد من تثبيت الحزم
npm install

# تأكد من وجود .env
# VITE_API_URL=http://localhost:8000/api

# تشغيل التطبيق
npm run dev
```

**✅ Frontend يعمل على:** `http://localhost:5173`

### 3. إعداد Postman

1. استورد ملف `POSTMAN_COLLECTION.json` في Postman
2. أنشئ Environment جديد:
   - `base_url`: `http://localhost:8000/api`
   - `token`: (سيتم ملؤه تلقائيًا)
   - `tenant_id`: `1`

---

## 📊 ملخص ما تم إنجازه

### ✅ اليوم 1: إعداد المشروع
- Multi-Tenant Structure
- BaseModel مع Global Scope
- CORS Configuration
- React Setup

### ✅ اليوم 2: المصادقة والصلاحيات
- Authentication (Register, Login, Logout)
- Roles & Permissions
- AuthContext
- Protected Routes

### ✅ اليوم 3: الأقسام والمنتجات
- Categories CRUD
- Products CRUD
- Product Search

### ✅ اليوم 4: المبيعات - الجزء الأول
- Inventory Transactions
- Sales System
- Cart Component

### ✅ اليوم 5: المبيعات - الجزء الثاني
- Invoice HTML Template
- Print Functionality

---

## 🧪 اختبارات Postman (خطوة بخطوة)

### الخطوة 1: تسجيل الدخول

**Request:**
```
POST http://localhost:8000/api/auth/login
Content-Type: application/json

{
  "email": "admin@example.com",
  "password": "password"
}
```

**✅ النجاح:**
- Status: 200
- Response يحتوي على `token` و `tenant_id`
- احفظ `token` في Environment variable

**⚠️ مهم:** بعد Login، استخدم هذا Token في جميع Requests التالية

---

### الخطوة 2: التحقق من المستخدم

**Request:**
```
GET http://localhost:8000/api/auth/me
Headers:
  Authorization: Bearer {token}
  X-Tenant-ID: 1
```

**✅ النجاح:**
- Status: 200
- Response يحتوي على بيانات المستخدم مع roles و permissions

---

### الخطوة 3: إنشاء قسم

**Request:**
```
POST http://localhost:8000/api/categories
Headers:
  Authorization: Bearer {token}
  X-Tenant-ID: 1
  Content-Type: application/json

Body:
{
  "name": "مشروبات",
  "description": "مشروبات غازية وعصائر"
}
```

**✅ النجاح:**
- Status: 201
- Response يحتوي على بيانات القسم الجديد
- **احفظ `id` من Response**

---

### الخطوة 4: عرض جميع الأقسام

**Request:**
```
GET http://localhost:8000/api/categories
Headers:
  Authorization: Bearer {token}
  X-Tenant-ID: 1
```

**✅ النجاح:**
- Status: 200
- Response يحتوي على قائمة الأقسام
- كل قسم يحتوي على `products_count`

---

### الخطوة 5: إنشاء منتج

**Request:**
```
POST http://localhost:8000/api/products
Headers:
  Authorization: Bearer {token}
  X-Tenant-ID: 1
  Content-Type: application/json

Body:
{
  "category_id": 1,
  "name": "كوكا كولا",
  "barcode": "123456789",
  "purchase_price": 2.5,
  "sale_price": 3.5,
  "quantity": 100,
  "expiry_date": "2026-12-31",
  "min_stock_alert": 10,
  "min_expiry_alert": 7
}
```

**✅ النجاح:**
- Status: 201
- Response يحتوي على بيانات المنتج الجديد
- **احفظ `id` من Response**

---

### الخطوة 6: البحث عن منتج

**Request:**
```
GET http://localhost:8000/api/products/search?q=كوكا
Headers:
  Authorization: Bearer {token}
  X-Tenant-ID: 1
```

**✅ النجاح:**
- Status: 200
- Response يحتوي على نتائج البحث

---

### الخطوة 7: إنشاء عملية بيع

**Request:**
```
POST http://localhost:8000/api/sales
Headers:
  Authorization: Bearer {token}
  X-Tenant-ID: 1
  Content-Type: application/json

Body:
{
  "items": [
    {
      "product_id": 1,
      "quantity": 2
    }
  ],
  "discount": 5,
  "discount_type": "fixed",
  "payment_method": "cash"
}
```

**✅ النجاح:**
- Status: 201
- Response يحتوي على بيانات البيع
- **احفظ `id` من Response**
- **احفظ `invoice_number`**

**✅ التحقق:**
- الكمية تُخصم من المخزون تلقائيًا
- يتم إنشاء Inventory Transaction

---

### الخطوة 8: عرض الفاتورة (HTML)

**Request:**
```
GET http://localhost:8000/api/sales/{id}/invoice
Headers:
  Authorization: Bearer {token}
  X-Tenant-ID: 1
```

**✅ النجاح:**
- Status: 200
- Response: HTML page جاهزة للطباعة

---

## 🖥️ اختبارات Frontend (خطوة بخطوة)

### الخطوة 1: تسجيل الدخول

1. افتح `http://localhost:5173`
2. سيتم توجيهك تلقائيًا إلى `/login`
3. استخدم:
   - **Email:** `admin@example.com`
   - **Password:** `password`
4. اضغط "تسجيل الدخول"

**✅ النجاح:**
- ✅ يتم توجيهك للصفحة الرئيسية
- ✅ يظهر اسم المستخدم في Header
- ✅ يظهر الدور (admin)
- ✅ تظهر قائمة Navigation

---

### الخطوة 2: إدارة الأقسام

1. اضغط على "الأقسام" في Navigation
2. اضغط "إضافة قسم جديد"
3. املأ النموذج:
   - **الاسم:** "مشروبات"
   - **الوصف:** "مشروبات غازية وعصائر"
4. اضغط "إضافة"

**✅ النجاح:**
- ✅ يظهر القسم الجديد في الجدول
- ✅ يمكنك تعديله (اضغط "تعديل")
- ✅ يمكنك حذفه (اضغط "حذف")

**🧪 اختبر:**
- حاول حذف قسم يحتوي على منتجات (يجب أن يظهر خطأ)

---

### الخطوة 3: إدارة المنتجات

1. اضغط على "المنتجات" في Navigation
2. اضغط "إضافة منتج جديد"
3. املأ النموذج:
   - **القسم:** اختر "مشروبات"
   - **اسم المنتج:** "كوكا كولا"
   - **الباركود:** "123456789"
   - **سعر الشراء:** 2.5
   - **سعر البيع:** 3.5
   - **الكمية:** 100
   - **تاريخ انتهاء الصلاحية:** 2026-12-31
4. اضغط "إضافة"

**✅ النجاح:**
- ✅ يظهر المنتج في الجدول
- ✅ يمكنك البحث عنه (اكتب "كوكا" في شريط البحث)
- ✅ يمكنك فلترته حسب القسم
- ✅ يمكنك تعديله
- ✅ يمكنك حذفه

**🧪 اختبر:**
- البحث بالاسم
- البحث بالباركود
- الفلترة حسب القسم

---

### الخطوة 4: شاشة المبيعات

1. اضغط على "المبيعات" في Navigation
2. ابحث عن منتج:
   - اكتب "كوكا" في شريط البحث
   - اضغط على المنتج لإضافته للسلة
3. عدل الكمية:
   - استخدم أزرار + و - أو اكتب الكمية مباشرة
4. أضف خصم (اختياري):
   - اختر نوع الخصم (نسبة/قيمة)
   - أدخل قيمة الخصم
5. اختر طريقة الدفع:
   - نقدي / بطاقة / تحويل
6. اضغط "إتمام البيع"

**✅ النجاح:**
- ✅ يتم إتمام البيع
- ✅ يتم توجيهك لصفحة الفاتورة
- ✅ تظهر الفاتورة بشكل احترافي

**🧪 اختبر:**
- إضافة منتجات متعددة
- تعديل الكميات
- حذف منتج من السلة
- إضافة خصم (نسبة)
- إضافة خصم (قيمة)
- تغيير طريقة الدفع

---

### الخطوة 5: طباعة الفاتورة

1. بعد إتمام البيع، ستظهر صفحة الفاتورة
2. اضغط "🖨️ طباعة"
3. اختر:
   - طباعة مباشرة
   - أو حفظ كـ PDF

**✅ النجاح:**
- ✅ تظهر الفاتورة بشكل احترافي
- ✅ يمكن طباعتها أو حفظها

---

## ✅ قائمة التحقق الشاملة

### Backend API (Postman)
- [ ] تسجيل الدخول
- [ ] `/api/auth/me`
- [ ] تسجيل الخروج
- [ ] إنشاء قسم
- [ ] عرض جميع الأقسام
- [ ] تعديل قسم
- [ ] حذف قسم
- [ ] إنشاء منتج
- [ ] عرض جميع المنتجات
- [ ] البحث عن منتج
- [ ] تعديل منتج
- [ ] حذف منتج
- [ ] إنشاء عملية بيع
- [ ] عرض جميع المبيعات
- [ ] عرض فاتورة (HTML)

### Frontend
- [ ] تسجيل الدخول
- [ ] تسجيل الخروج
- [ ] إدارة الأقسام (إضافة/تعديل/حذف)
- [ ] إدارة المنتجات (إضافة/تعديل/حذف)
- [ ] البحث عن منتج
- [ ] الفلترة حسب القسم
- [ ] شاشة المبيعات
- [ ] إضافة منتج للسلة
- [ ] تعديل الكمية
- [ ] حذف منتج من السلة
- [ ] إضافة خصم
- [ ] اختيار طريقة الدفع
- [ ] إتمام البيع
- [ ] عرض الفاتورة
- [ ] طباعة الفاتورة

### Integration
- [ ] Token يتم إرساله تلقائيًا
- [ ] tenant_id يتم إرساله تلقائيًا
- [ ] عند 401، يتم توجيه المستخدم للـ login
- [ ] Error Handling يعمل

### Multi-Tenant
- [ ] Global Scope يعمل
- [ ] لا يمكن رؤية بيانات tenant آخر

---

## 🐛 حل المشاكل

### مشكلة: "No connection could be made"
**الحل:** تأكد من تشغيل MySQL

### مشكلة: CORS Error
**الحل:** 
- تأكد من `config/cors.php`
- تأكد من `config/sanctum.php`

### مشكلة: Token غير صالح
**الحل:**
- سجل دخول مرة أخرى
- احفظ Token الجديد

### مشكلة: "Tenant not identified"
**الحل:**
- أضف Header: `X-Tenant-ID: 1`
- أو تأكد من أن المستخدم له `tenant_id`

---

## 📝 ملاحظات مهمة

1. **Token**: يجب استخدام Token في جميع Requests المحمية
2. **tenant_id**: يجب إرسال `X-Tenant-ID` header
3. **Global Scope**: كل tenant يرى بياناته فقط
4. **Inventory Transactions**: يتم إنشاؤها تلقائيًا عند البيع
5. **Invoice**: HTML template جاهز للطباعة

---

**جاهز للاختبار! 🚀**
